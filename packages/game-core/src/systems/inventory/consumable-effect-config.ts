/** 当前模块对外公开的只读配置值。 */
export const CONSUMABLE_EFFECT_IDS = ["resource.restore", "status.add", "status.remove"] as const;

/** 当前模块对外公开的只读配置值。 */
export const CONSUMABLE_EFFECT_EXECUTION_OUTCOMES = ["applied", "skipped"] as const;
