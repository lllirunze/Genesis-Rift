# Hand Cards

未来保存手牌系统中的手牌配置资源。

所有手牌共用一套从 `1` 开始的全局数字编号。功能相同的多张手牌分别保存为独立配置，除 `cardId` 外可以直接复制相同的名称、描述与效果字段。

配置中的 `name` 统一使用英文小驼峰命名法（camelCase），中文名称直接绘制在对应的牌面资源图片中。

配置中的 `description` 使用首字母大写且带英文句末标点的完整英文句子，中文说明同样由牌面资源图片负责展示。
