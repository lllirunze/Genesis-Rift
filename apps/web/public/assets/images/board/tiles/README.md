# 地图基础地形资源

本目录仅存放六边形地块的基础地形图片，不包含道路、城镇、遗迹等附属要素。

每种基础地形使用独立文件夹，文件名统一采用：

```text
<terrain-name>-<two-digit-variant>.avif
```

例如：

```text
forest-01.avif
forest-02.avif
```

同一地形可持续增加变体。地图渲染层后续应根据稳定地块标识选择变体，使同一地块始终显示相同图片。
